# Week 1 / Session 1

The Week 1 source document contains five live-coding problems. They are placed under
`class_problems`.

The GitHub Structure Guide requires a topic package containing:
- `class_problems`
- `assigment_problems`

The Week 1 problem sheet does not specify the official topic/package name, so `week1`
is used here as a temporary topic name. If your mentor gave a specific topic name,
rename the `week1` package/folder and update the Java `package` declarations accordingly.
